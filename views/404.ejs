<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Page not found" />
    <title>404 - Page Not Found | Vendi</title>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
    />

    <style>
      :root {
        --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        --primary-color: #667eea;
        --text-dark: #2d3748;
        --text-muted: #718096;
      }

      body {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        font-family: "Inter", sans-serif;
        color: var(--text-dark);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem;
      }

      .error-container {
        max-width: 600px;
        text-align: center;
      }

      .error-card {
        background: white;
        border-radius: 24px;
        padding: 4rem 3rem;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      }

      .error-number {
        font-size: 8rem;
        font-weight: 800;
        background: var(--primary-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        line-height: 1;
        margin-bottom: 1rem;
        animation: bounce 2s infinite;
      }

      @keyframes bounce {
        0%,
        100% {
          transform: translateY(0);
        }
        50% {
          transform: translateY(-20px);
        }
      }

      .error-title {
        font-size: 2.5rem;
        font-weight: 800;
        margin-bottom: 1rem;
        color: var(--text-dark);
      }

      .error-message {
        font-size: 1.2rem;
        color: var(--text-muted);
        margin-bottom: 2rem;
        line-height: 1.6;
      }

      .error-icon {
        width: 120px;
        height: 120px;
        margin: 0 auto 2rem;
        opacity: 0.3;
      }

      .btn-home {
        background: var(--primary-gradient);
        border: none;
        border-radius: 25px;
        padding: 1rem 2.5rem;
        font-weight: 600;
        color: white;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 0.8rem;
        transition: all 0.3s;
        box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
      }

      .btn-home:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 25px rgba(102, 126, 234, 0.4);
        color: white;
      }

      .suggestions {
        margin-top: 3rem;
        padding-top: 2rem;
        border-top: 2px solid #f7fafc;
      }

      .suggestions h5 {
        font-weight: 700;
        margin-bottom: 1.5rem;
        color: var(--text-dark);
      }

      .suggestion-links {
        display: flex;
        flex-direction: column;
        gap: 1rem;
      }

      .suggestion-link {
        color: var(--primary-color);
        text-decoration: none;
        font-weight: 600;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        padding: 0.8rem;
        border-radius: 12px;
        transition: all 0.3s;
      }

      .suggestion-link:hover {
        background: #f7fafc;
        color: var(--primary-color);
      }

      @media (max-width: 576px) {
        .error-number {
          font-size: 6rem;
        }
        .error-title {
          font-size: 2rem;
        }
        .error-card {
          padding: 3rem 2rem;
        }
      }
    </style>
  </head>

  <body>
    <div class="error-container">
      <div class="error-card">
        <!-- SVG Icon -->
        <svg
          class="error-icon"
          viewBox="0 0 200 200"
          xmlns="http://www.w3.org/2000/svg"
        >
          <circle
            cx="100"
            cy="100"
            r="80"
            fill="none"
            stroke="url(#gradient)"
            stroke-width="8"
          />
          <path
            d="M70 80 L130 80 M70 120 L130 120"
            stroke="url(#gradient)"
            stroke-width="8"
            stroke-linecap="round"
          />
          <circle cx="75" cy="60" r="8" fill="url(#gradient)" />
          <circle cx="125" cy="60" r="8" fill="url(#gradient)" />
          <defs>
            <linearGradient id="gradient" x1="0%" y1="0%" x2="100%" y2="100%">
              <stop offset="0%" style="stop-color: #667eea; stop-opacity: 1" />
              <stop
                offset="100%"
                style="stop-color: #764ba2; stop-opacity: 1"
              />
            </linearGradient>
          </defs>
        </svg>

        <div class="error-number">404</div>
        <h1 class="error-title">Page Not Found</h1>
        <p class="error-message">
          Oops! The page you're looking for doesn't exist. It might have been
          moved or deleted.
        </p>

        <a href="/" class="btn-home">
          <i class="fas fa-home"></i>
          Back to Home
        </a>

        <div class="suggestions">
          <h5>You might be looking for:</h5>
          <div class="suggestion-links">
            <a href="/admin/ads" class="suggestion-link">
              <i class="fas fa-search"></i>
              Browse All Listings
            </a>
            <a href="/about" class="suggestion-link">
              <i class="fas fa-info-circle"></i>
              About Vendi
            </a>
            <a href="/contact" class="suggestion-link">
              <i class="fas fa-envelope"></i>
              Contact Support
            </a>
            <a href="/support/help-center" class="suggestion-link">
              <i class="fas fa-question-circle"></i>
              Help Center
            </a>
          </div>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
