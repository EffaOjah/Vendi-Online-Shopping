<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Chat with buyers and sellers on Vendi" />
    <title>Vendi - Messages</title>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
    />

    <style>
      :root {
        --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        --primary-color: #667eea;
        --text-dark: #2d3748;
        --text-muted: #718096;
        --border-radius: 16px;
        --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      }

      body {
        background: #f7fafc;
        font-family: "Inter", sans-serif;
        color: var(--text-dark);
        height: 100vh;
        display: flex;
        flex-direction: column;
        overflow: hidden;
      }

      .navbar {
        backdrop-filter: blur(10px);
        background: rgba(255, 255, 255, 0.95) !important;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
        padding: 1rem 0;
        flex-shrink: 0;
        z-index: 1000;
      }

      .navbar-brand {
        font-weight: 800;
        font-size: 1.5rem;
        background: var(--primary-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .nav-link {
        font-weight: 500;
        color: var(--text-dark) !important;
        margin: 0 0.5rem;
        transition: var(--transition);
      }

      .nav-link:hover {
        color: var(--primary-color) !important;
      }

      .btn-primary {
        background: var(--primary-gradient);
        border: none;
        border-radius: 25px;
        padding: 0.6rem 1.8rem;
        font-weight: 600;
        transition: var(--transition);
        box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
      }

      .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 25px rgba(102, 126, 234, 0.4);
      }

      .chat-container {
        flex-grow: 1;
        display: flex;
        overflow: hidden;
        background: white;
        margin: 1rem;
        border-radius: var(--border-radius);
        box-shadow: 0 5px 20px rgba(0, 0, 0, 0.05);
      }

      /* Sidebar */
      .chat-sidebar {
        width: 350px;
        border-right: 1px solid #e2e8f0;
        display: flex;
        flex-direction: column;
        background: white;
      }

      .sidebar-header {
        padding: 1.5rem;
        border-bottom: 1px solid #e2e8f0;
      }

      .search-bar {
        position: relative;
      }

      .search-bar input {
        width: 100%;
        padding: 0.8rem 1rem 0.8rem 2.5rem;
        border: 1px solid #e2e8f0;
        border-radius: 25px;
        background: #f7fafc;
      }

      .search-bar i {
        position: absolute;
        left: 1rem;
        top: 50%;
        transform: translateY(-50%);
        color: var(--text-muted);
      }

      .conversation-list {
        flex-grow: 1;
        overflow-y: auto;
      }

      .conversation-item {
        padding: 1rem 1.5rem;
        display: flex;
        gap: 1rem;
        cursor: pointer;
        transition: var(--transition);
        border-bottom: 1px solid #f7fafc;
      }

      .conversation-item:hover {
        background: #f7fafc;
      }

      .conversation-item.active {
        background: #ebf4ff;
        border-left: 4px solid var(--primary-color);
      }

      .user-avatar {
        width: 50px;
        height: 50px;
        border-radius: 50%;
        object-fit: cover;
        flex-shrink: 0;
      }

      .conversation-info {
        flex-grow: 1;
        min-width: 0;
      }

      .conversation-top {
        display: flex;
        justify-content: space-between;
        margin-bottom: 0.3rem;
      }

      .user-name {
        font-weight: 600;
        color: var(--text-dark);
      }

      .time {
        font-size: 0.8rem;
        color: var(--text-muted);
      }

      .last-message {
        color: var(--text-muted);
        font-size: 0.9rem;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
      }

      .unread-badge {
        background: #e53e3e;
        color: white;
        font-size: 0.7rem;
        padding: 0.1rem 0.4rem;
        border-radius: 10px;
        margin-left: 0.5rem;
      }

      /* Main Chat Area */
      .chat-main {
        flex-grow: 1;
        display: flex;
        flex-direction: column;
        background: #f8f9fa;
      }

      .chat-header {
        padding: 1rem 2rem;
        background: white;
        border-bottom: 1px solid #e2e8f0;
        display: flex;
        align-items: center;
        justify-content: space-between;
      }

      .chat-user-info {
        display: flex;
        align-items: center;
        gap: 1rem;
      }

      .status-indicator {
        width: 10px;
        height: 10px;
        background: #48bb78;
        border-radius: 50%;
        border: 2px solid white;
        position: absolute;
        bottom: 0;
        right: 0;
      }

      .product-context {
        display: flex;
        align-items: center;
        gap: 1rem;
        background: #f7fafc;
        padding: 0.8rem 1.5rem;
        border-bottom: 1px solid #e2e8f0;
      }

      .product-thumb {
        width: 40px;
        height: 40px;
        border-radius: 8px;
        object-fit: cover;
      }

      .messages-area {
        flex-grow: 1;
        padding: 2rem;
        overflow-y: auto;
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
      }

      .message {
        display: flex;
        max-width: 70%;
      }

      .message.received {
        align-self: flex-start;
      }

      .message.sent {
        align-self: flex-end;
        flex-direction: row-reverse;
      }

      .message-bubble {
        padding: 1rem;
        border-radius: 16px;
        position: relative;
        line-height: 1.5;
      }

      .received .message-bubble {
        background: white;
        border-bottom-left-radius: 4px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.05);
      }

      .sent .message-bubble {
        background: var(--primary-gradient);
        color: white;
        border-bottom-right-radius: 4px;
        box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
      }

      .message-time {
        font-size: 0.75rem;
        color: var(--text-muted);
        margin-top: 0.4rem;
      }

      .sent .message-time {
        text-align: right;
      }

      .chat-input-area {
        padding: 1.5rem;
        background: white;
        border-top: 1px solid #e2e8f0;
      }

      .input-wrapper {
        display: flex;
        gap: 1rem;
        align-items: center;
        background: #f7fafc;
        padding: 0.5rem 1rem;
        border-radius: 25px;
        border: 1px solid #e2e8f0;
      }

      .chat-input {
        flex-grow: 1;
        border: none;
        background: transparent;
        padding: 0.5rem;
        outline: none;
        max-height: 100px;
        resize: none;
      }

      .action-btn {
        background: none;
        border: none;
        color: var(--text-muted);
        cursor: pointer;
        padding: 0.5rem;
        transition: var(--transition);
      }

      .action-btn:hover {
        color: var(--primary-color);
      }

      .send-btn {
        background: var(--primary-gradient);
        color: white;
        border: none;
        width: 40px;
        height: 40px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: var(--transition);
      }

      .send-btn:hover {
        transform: scale(1.05);
      }

      @media (max-width: 768px) {
        .chat-container {
          margin: 0;
          border-radius: 0;
        }
        .chat-sidebar {
          width: 100%;
          display: none; /* Hide sidebar on mobile when chat is open */
        }
        .chat-sidebar.active {
          display: flex;
        }
        .chat-main.hidden {
          display: none;
        }
      }
    </style>
  </head>

  <body>
    <!-- NAVBAR -->
    <nav class="navbar navbar-expand-lg sticky-top">
      <div class="container">
        <a class="navbar-brand" href="/">
          <img src="/images/vendi-logo.png" alt="" width="80px" />
        </a>

        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navMenu"
        >
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navMenu">
          <ul class="navbar-nav ms-auto align-items-center">
            <li class="nav-item">
              <a href="/" class="nav-link">Home</a>
            </li>
            <li class="nav-item">
              <a href="/user/my-ads" class="nav-link">My Ads</a>
            </li>
            <li class="nav-item">
              <a href="/user/chat" class="nav-link">Messages</a>
            </li>
            <li class="nav-item">
              <a href="/user/saved-items" class="nav-link">
                <i class="far fa-heart me-1"></i>Saved
              </a>
            </li>
            <li class="nav-item dropdown">
              <a
                class="nav-link dropdown-toggle"
                href="#"
                role="button"
                data-bs-toggle="dropdown"
              >
                <img
                  src="https://ui-avatars.com/api/?name=John+Doe&background=random"
                  class="rounded-circle me-1"
                  width="32"
                  height="32"
                  alt="User"
                />
              </a>
              <ul
                class="dropdown-menu dropdown-menu-end border-0 shadow-lg rounded-4 mt-2"
              >
                <li>
                  <a class="dropdown-item py-2" href="/user/profile"
                    ><i class="fas fa-user me-2"></i>Profile</a
                  >
                </li>
                <li>
                  <a class="dropdown-item py-2" href="/user/settings"
                    ><i class="fas fa-cog me-2"></i>Settings</a
                  >
                </li>
                <li><hr class="dropdown-divider" /></li>
                <li>
                  <a class="dropdown-item py-2 text-danger" href="/auth/login"
                    ><i class="fas fa-sign-out-alt me-2"></i>Logout</a
                  >
                </li>
              </ul>
            </li>
            <li class="nav-item">
              <a href="/user/post-ad" class="btn btn-primary ms-2">
                <i class="fas fa-plus me-2"></i>Post Ad
              </a>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <!-- CHAT INTERFACE -->
    <div class="chat-container">
      <!-- Sidebar -->
      <div class="chat-sidebar">
        <div class="sidebar-header">
          <h4 class="fw-bold mb-3">Messages</h4>
          <div class="search-bar">
            <i class="fas fa-search"></i>
            <input type="text" placeholder="Search conversations..." />
          </div>
        </div>

        <div class="conversation-list">
          <!-- Item 1 -->
          <div class="conversation-item active">
            <div style="position: relative">
              <img
                src="https://ui-avatars.com/api/?name=Adebayo+Johnson&background=random"
                class="user-avatar"
                alt="User"
              />
              <div class="status-indicator"></div>
            </div>
            <div class="conversation-info">
              <div class="conversation-top">
                <span class="user-name">Adebayo Johnson</span>
                <span class="time">10:30 AM</span>
              </div>
              <div class="last-message fw-bold">
                Is the iPhone still available?
                <span class="unread-badge">2</span>
              </div>
            </div>
          </div>

          <!-- Item 2 -->
          <div class="conversation-item">
            <img
              src="https://ui-avatars.com/api/?name=Chioma+Okafor&background=random"
              class="user-avatar"
              alt="User"
            />
            <div class="conversation-info">
              <div class="conversation-top">
                <span class="user-name">Chioma Okafor</span>
                <span class="time">Yesterday</span>
              </div>
              <div class="last-message">Thanks for the quick delivery!</div>
            </div>
          </div>

          <!-- Item 3 -->
          <div class="conversation-item">
            <img
              src="https://ui-avatars.com/api/?name=Tunde+Bakare&background=random"
              class="user-avatar"
              alt="User"
            />
            <div class="conversation-info">
              <div class="conversation-top">
                <span class="user-name">Tunde Bakare</span>
                <span class="time">Mon</span>
              </div>
              <div class="last-message">Can we meet at Ikeja City Mall?</div>
            </div>
          </div>
        </div>
      </div>

      <!-- Main Chat -->
      <div class="chat-main">
        <!-- Header -->
        <div class="chat-header">
          <div class="chat-user-info">
            <img
              src="https://ui-avatars.com/api/?name=Adebayo+Johnson&background=random"
              class="user-avatar"
              style="width: 40px; height: 40px"
              alt="User"
            />
            <div>
              <h6 class="mb-0 fw-bold">Adebayo Johnson</h6>
              <small class="text-success">Online</small>
            </div>
          </div>
          <div class="chat-actions">
            <button class="btn btn-light btn-sm rounded-circle">
              <i class="fas fa-phone"></i>
            </button>
            <button class="btn btn-light btn-sm rounded-circle ms-2">
              <i class="fas fa-ellipsis-v"></i>
            </button>
          </div>
        </div>

        <!-- Product Context -->
        <div class="product-context">
          <img
            src="https://images.unsplash.com/photo-1511707171634-5f897ff02aa9?w=100"
            class="product-thumb"
            alt="Product"
          />
          <div>
            <div class="fw-bold small">iPhone 15 Pro Max 256GB</div>
            <div class="text-primary small fw-bold">₦1,250,000</div>
          </div>
        </div>

        <!-- Messages -->
        <div class="messages-area" id="messagesArea">
          <div class="text-center text-muted small mb-3">Today</div>

          <div class="message received">
            <img
              src="https://ui-avatars.com/api/?name=Adebayo+Johnson&background=random"
              class="user-avatar me-2"
              style="width: 32px; height: 32px"
              alt="User"
            />
            <div>
              <div class="message-bubble">
                Hello! I saw your ad for the iPhone 15 Pro Max.
              </div>
              <div class="message-time">10:28 AM</div>
            </div>
          </div>

          <div class="message received">
            <img
              src="https://ui-avatars.com/api/?name=Adebayo+Johnson&background=random"
              class="user-avatar me-2"
              style="width: 32px; height: 32px"
              alt="User"
            />
            <div>
              <div class="message-bubble">Is it still available for sale?</div>
              <div class="message-time">10:30 AM</div>
            </div>
          </div>

          <div class="message sent">
            <div>
              <div class="message-bubble">
                Hi Adebayo! Yes, it is still available.
              </div>
              <div class="message-time">10:32 AM</div>
            </div>
          </div>
        </div>

        <!-- Input -->
        <div class="chat-input-area">
          <div class="input-wrapper">
            <button class="action-btn"><i class="fas fa-paperclip"></i></button>
            <button class="action-btn"><i class="far fa-image"></i></button>
            <textarea
              class="chat-input"
              placeholder="Type a message..."
              rows="1"
            ></textarea>
            <button class="send-btn"><i class="fas fa-paper-plane"></i></button>
          </div>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
      // Auto-resize textarea
      const textarea = document.querySelector(".chat-input");
      textarea.addEventListener("input", function () {
        this.style.height = "auto";
        this.style.height = this.scrollHeight + "px";
      });

      // Scroll to bottom
      const messagesArea = document.getElementById("messagesArea");
      messagesArea.scrollTop = messagesArea.scrollHeight;
    </script>
  </body>
</html>
