<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Account Settings - Vendi" />
    <title>Vendi - Account Settings</title>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
    />

    <style>
      :root {
        --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        --primary-color: #667eea;
        --text-dark: #2d3748;
        --text-muted: #718096;
        --border-radius: 16px;
        --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        --sidebar-width: 280px;
      }

      body {
        background: #f7fafc;
        font-family: "Inter", sans-serif;
        color: var(--text-dark);
        min-height: 100vh;
        display: flex;
      }

      /* Sidebar (Same as Dashboard) */
      .sidebar {
        width: var(--sidebar-width);
        background: white;
        height: 100vh;
        position: fixed;
        left: 0;
        top: 0;
        border-right: 1px solid #e2e8f0;
        display: flex;
        flex-direction: column;
        z-index: 1000;
        transition: var(--transition);
      }

      .sidebar-header {
        padding: 1.5rem 2rem;
        border-bottom: 1px solid #e2e8f0;
      }

      .brand-logo {
        font-weight: 800;
        font-size: 1.5rem;
        background: var(--primary-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        text-decoration: none;
      }

      .sidebar-menu {
        padding: 1.5rem 1rem;
        flex-grow: 1;
        overflow-y: auto;
      }

      .menu-label {
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 1px;
        color: var(--text-muted);
        margin: 0 1rem 0.8rem;
        font-weight: 600;
      }

      .nav-item {
        margin-bottom: 0.5rem;
      }

      .nav-link {
        display: flex;
        align-items: center;
        padding: 0.8rem 1rem;
        color: var(--text-dark);
        border-radius: 12px;
        font-weight: 500;
        transition: var(--transition);
        text-decoration: none;
      }

      .nav-link:hover,
      .nav-link.active {
        background: #ebf4ff;
        color: var(--primary-color);
      }

      .nav-link i {
        width: 24px;
        margin-right: 0.8rem;
        font-size: 1.1rem;
      }

      .sidebar-footer {
        padding: 1.5rem;
        border-top: 1px solid #e2e8f0;
      }

      /* Main Content */
      .main-content {
        margin-left: var(--sidebar-width);
        flex-grow: 1;
        padding: 2rem;
        transition: var(--transition);
      }

      .top-bar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 2rem;
      }

      .page-title {
        font-size: 1.8rem;
        font-weight: 800;
        margin: 0;
      }

      .content-card {
        background: white;
        border-radius: var(--border-radius);
        padding: 2rem;
        box-shadow: 0 5px 20px rgba(0, 0, 0, 0.05);
        margin-bottom: 2rem;
      }

      .section-title {
        font-size: 1.1rem;
        font-weight: 700;
        margin-bottom: 1.5rem;
        color: var(--text-dark);
      }

      .setting-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 1.5rem 0;
        border-bottom: 1px solid #f7fafc;
      }

      .setting-item:last-child {
        border-bottom: none;
      }

      .setting-info h6 {
        font-weight: 600;
        margin-bottom: 0.3rem;
      }

      .setting-info p {
        color: var(--text-muted);
        margin: 0;
        font-size: 0.9rem;
      }

      .payment-card {
        border: 1px solid #e2e8f0;
        border-radius: 12px;
        padding: 1.5rem;
        display: flex;
        align-items: center;
        gap: 1rem;
        margin-bottom: 1rem;
        transition: var(--transition);
      }

      .payment-card:hover {
        border-color: var(--primary-color);
        background: #f7fafc;
      }

      .card-icon {
        font-size: 2rem;
        color: var(--text-dark);
      }

      .btn-outline-primary {
        border-radius: 12px;
        font-weight: 600;
        padding: 0.6rem 1.5rem;
      }

      .form-select {
        border-radius: 12px;
        padding: 0.6rem 2rem 0.6rem 1rem;
        border: 2px solid #e2e8f0;
        cursor: pointer;
      }

      .form-select:focus {
        border-color: var(--primary-color);
        box-shadow: none;
      }

      @media (max-width: 992px) {
        .sidebar {
          transform: translateX(-100%);
        }
        .sidebar.active {
          transform: translateX(0);
        }
        .main-content {
          margin-left: 0;
        }
      }
    </style>
  </head>

  <body>
    <!-- Sidebar -->
    <aside class="sidebar" id="sidebar">
      <div class="sidebar-header">
        <a href="/home" class="brand-logo">
          <img src="/images/vendi-logo.png" alt="Vendi" width="100" />
        </a>
      </div>

      <div class="sidebar-menu">
        <div class="menu-label">Menu</div>
        <div class="nav-item">
          <a href="/user/dashboard" class="nav-link">
            <i class="fas fa-th-large"></i>Dashboard
          </a>
        </div>
        <div class="nav-item">
          <a href="/user/my-ads" class="nav-link">
            <i class="fas fa-ad"></i>My Ads
          </a>
        </div>
        <div class="nav-item">
          <a href="/user/chat" class="nav-link">
            <i class="fas fa-comments"></i>Messages
            <span class="badge bg-danger rounded-pill ms-auto">2</span>
          </a>
        </div>
        <div class="nav-item">
          <a href="/user/saved-items" class="nav-link">
            <i class="fas fa-heart"></i>Saved Items
          </a>
        </div>

        <div class="menu-label mt-4">Account</div>
        <div class="nav-item">
          <a href="/user/profile" class="nav-link">
            <i class="fas fa-user"></i>Profile
          </a>
        </div>
        <div class="nav-item">
          <a href="/user/settings" class="nav-link active">
            <i class="fas fa-cog"></i>Settings
          </a>
        </div>
        <div class="nav-item">
          <a href="/auth/logout" class="nav-link text-danger">
            <i class="fas fa-sign-out-alt"></i>Logout
          </a>
        </div>
      </div>

      <div class="sidebar-footer">
        <div class="d-grid">
          <a
            href="/user/post-ad"
            class="btn btn-primary"
            style="
              background: var(--primary-gradient);
              border: none;
              border-radius: 12px;
              padding: 0.8rem;
              font-weight: 600;
              color: white;
              text-decoration: none;
              text-align: center;
            "
          >
            <i class="fas fa-plus me-2"></i>Post New Ad
          </a>
        </div>
      </div>
    </aside>

    <!-- Main Content -->
    <main class="main-content">
      <button
        class="btn btn-light d-lg-none mb-3"
        onclick="document.getElementById('sidebar').classList.toggle('active')"
      >
        <i class="fas fa-bars"></i>
      </button>

      <div class="top-bar">
        <h1 class="page-title">Account Settings</h1>
      </div>

      <!-- Privacy Settings -->
      <div class="content-card">
        <h5 class="section-title">Privacy & Visibility</h5>

        <div class="setting-item">
          <div class="setting-info">
            <h6>Profile Visibility</h6>
            <p>Choose who can see your profile details</p>
          </div>
          <select class="form-select" style="width: auto">
            <option>Everyone</option>
            <option>Registered Users</option>
            <option>Only Me</option>
          </select>
        </div>

        <div class="setting-item">
          <div class="setting-info">
            <h6>Show Online Status</h6>
            <p>Let others know when you're active</p>
          </div>
          <div class="form-check form-switch">
            <input
              class="form-check-input"
              type="checkbox"
              checked
              style="width: 3em; height: 1.5em"
            />
          </div>
        </div>

        <div class="setting-item">
          <div class="setting-info">
            <h6>Show Phone Number</h6>
            <p>Display your phone number on your public profile</p>
          </div>
          <div class="form-check form-switch">
            <input
              class="form-check-input"
              type="checkbox"
              style="width: 3em; height: 1.5em"
            />
          </div>
        </div>
      </div>

      <!-- Payment Methods -->
      <div class="content-card">
        <div class="d-flex justify-content-between align-items-center mb-4">
          <h5 class="section-title mb-0">Payment Methods</h5>
          <button class="btn btn-outline-primary btn-sm">
            <i class="fas fa-plus me-2"></i>Add New
          </button>
        </div>

        <div class="payment-card">
          <i class="fab fa-cc-mastercard card-icon text-warning"></i>
          <div class="flex-grow-1">
            <h6 class="mb-0 fw-bold">Mastercard ending in 4242</h6>
            <small class="text-muted">Expires 12/25</small>
          </div>
          <span class="badge bg-light text-dark">Default</span>
          <button class="btn btn-link text-danger p-0 ms-3">
            <i class="fas fa-trash"></i>
          </button>
        </div>

        <div class="payment-card">
          <i class="fab fa-cc-visa card-icon text-primary"></i>
          <div class="flex-grow-1">
            <h6 class="mb-0 fw-bold">Visa ending in 8899</h6>
            <small class="text-muted">Expires 09/26</small>
          </div>
          <button class="btn btn-link text-danger p-0 ms-3">
            <i class="fas fa-trash"></i>
          </button>
        </div>
      </div>

      <!-- Preferences -->
      <div class="content-card">
        <h5 class="section-title">Preferences</h5>

        <div class="setting-item">
          <div class="setting-info">
            <h6>Language</h6>
            <p>Select your preferred language</p>
          </div>
          <select class="form-select" style="width: auto">
            <option>English (US)</option>
            <option>French</option>
            <option>Spanish</option>
          </select>
        </div>

        <div class="setting-item">
          <div class="setting-info">
            <h6>Currency</h6>
            <p>Choose your preferred currency for display</p>
          </div>
          <select class="form-select" style="width: auto">
            <option>NGN (₦)</option>
            <option>USD ($)</option>
            <option>EUR (€)</option>
          </select>
        </div>

        <div class="setting-item">
          <div class="setting-info">
            <h6>Time Zone</h6>
            <p>Set your local time zone</p>
          </div>
          <select class="form-select" style="width: auto">
            <option>(GMT+01:00) West Central Africa</option>
            <option>(GMT+00:00) London</option>
            <option>(GMT-05:00) Eastern Time</option>
          </select>
        </div>
      </div>

      <!-- Connected Accounts -->
      <div class="content-card">
        <h5 class="section-title">Connected Accounts</h5>

        <div class="setting-item">
          <div class="setting-info">
            <div class="d-flex align-items-center gap-2">
              <i class="fab fa-google text-danger"></i>
              <h6 class="mb-0">Google</h6>
            </div>
            <p class="mt-1">Connected as john.doe@gmail.com</p>
          </div>
          <button class="btn btn-outline-danger btn-sm">Disconnect</button>
        </div>

        <div class="setting-item">
          <div class="setting-info">
            <div class="d-flex align-items-center gap-2">
              <i class="fab fa-facebook text-primary"></i>
              <h6 class="mb-0">Facebook</h6>
            </div>
            <p class="mt-1">Not connected</p>
          </div>
          <button class="btn btn-outline-primary btn-sm">Connect</button>
        </div>
      </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
